package strategy.botplayingstrategy;

import model.Board;
import model.Move;

public class MediumLevelStrategy implements BotPlayingStrategy{
    @Override
    public Move makeMove(Board board) {
        return null;
    }
}
