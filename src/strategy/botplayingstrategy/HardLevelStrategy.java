package strategy.botplayingstrategy;


import model.Board;
import model.Move;

public class HardLevelStrategy implements BotPlayingStrategy {
    @Override
    public Move makeMove(Board board) {
        return null;
    }
}
